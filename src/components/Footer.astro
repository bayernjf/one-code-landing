---
import { useTranslations, localizePath, type Lang } from '../i18n';
import { SITE_NAME, SOCIAL } from '../consts';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = useTranslations(lang);
const year = new Date().getFullYear();

const links = [
  { href: localizePath('/#install', lang), label: t('nav.install') },
  { href: localizePath('/changelog', lang), label: t('nav.changelog') },
  { href: localizePath('/#faq', lang), label: t('nav.faq') },
  { href: SOCIAL.github, label: 'GitHub', external: true },
  { href: 'https://bayjf.com', label: 'BayJF', external: true },
  { href: localizePath('/privacy', lang), label: lang === 'zh' ? '隐私政策' : 'Privacy' },
  { href: localizePath('/terms', lang), label: lang === 'zh' ? '服务条款' : 'Terms' },
];
---

<footer class="border-t border-ink-800/60 bg-ink-950">
  <div class="mx-auto max-w-6xl px-4 py-12 sm:px-6">
    <div class="flex flex-col items-center justify-between gap-6 sm:flex-row">
      <div class="flex items-center gap-2">
        <svg viewBox="0 0 32 32" class="h-6 w-6" fill="none" aria-hidden="true">
          <rect width="32" height="32" rx="7" fill="#f59e0b" />
          <path d="M16 7L7 11v6c0 4.5 3.8 8.3 9 9.5 5.2-1.2 9-5 9-9.5v-6L16 7z" fill="#fff" fill-opacity="0.15" stroke="#fff" stroke-width="1.5" stroke-linejoin="round" />
          <circle cx="12.5" cy="14.5" r="1.8" fill="#fff" />
          <circle cx="19.5" cy="14.5" r="1.8" fill="#fff" />
          <path d="M12 19.5c1 1.5 2.5 2.5 4 2.5s3-1 4-2.5" stroke="#fff" stroke-width="1.5" stroke-linecap="round" />
        </svg>
        <div>
          <p class="text-sm font-medium text-ink-100">{SITE_NAME}</p>
          <p class="text-xs text-ink-500">{t('footer.tagline')}</p>
        </div>
      </div>

      <nav class="flex flex-wrap items-center justify-center gap-5">
        {links.map((l) => (
          <a
            href={l.href}
            class="text-xs text-ink-400 transition hover:text-ink-100"
          >
            {l.label}
          </a>
        ))}
      </nav>
    </div>

    <div class="mt-8 border-t border-ink-800/50 pt-6 text-center">
      <p class="text-xs text-ink-500">
        © {year} {SITE_NAME}. {t('footer.rights')}
      </p>
    </div>
  </div>
</footer>
